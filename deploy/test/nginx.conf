events {
}

http {
    include mime.types;
    server {

        listen 8443 default_server;
        server_name localhost;

        location / {
            proxy_pass http://bot:8080/;
        }
    }
}